# Kong API Gateway Production Environment Variables
# Generated for production deployment
# 
# IMPORTANT SECURITY NOTES:
# 1. Change ALL default values below
# 2. Use strong passwords (minimum 32 characters)
# 3. Keep this file secure and never commit to version control
# 4. Rotate these secrets regularly (every 90 days)

# Database Configuration - REQUIRED
# Use a cryptographically secure random password
DB_PASSWORD=CHANGE_THIS_TO_VERY_LONG_SECURE_DATABASE_PASSWORD_MIN_32_CHARS

# Kong Security Configuration - REQUIRED
# Session secret should be minimum 64 characters
KONG_SESSION_SECRET=CHANGE_THIS_TO_VERY_LONG_SESSION_SECRET_MIN_64_CHARS_FOR_JWT_SIGNING

# Admin password should be minimum 32 characters
KONG_ADMIN_PASSWORD=CHANGE_THIS_TO_VERY_LONG_ADMIN_PASSWORD_MIN_32_CHARS

# SSL Configuration (if using custom certificates)
KONG_SSL_CERT_PATH=/etc/kong/ssl/kong.crt
KONG_SSL_KEY_PATH=/etc/kong/ssl/kong.key

# Additional Production Settings
KONG_VERSION=3.7

# Generate secure passwords using:
# openssl rand -base64 48   # For passwords
# openssl rand -base64 64   # For session secrets